import java.lang.reflect.Array;
public class Board
{
    private Array[][] Board;
    private int numRows;
    private int numCols;

    public Board(int rows, int cols)
    {
        numRows = rows;
        numCols = cols;
        Board = new Array[rows][cols];
        for(int i = )
    }
}
